package April.ex_18042024;

public class L041_typecasting_impl {
    public static void main(String[] args) {
        // Casting -> molding

        byte b = 5;
        int a = 5;


        int d = b; // means int d = (int) b, is known as Implicit casting and which is done by JVM automatically. because we are transferring the small basket to big basket.
      //  byte e = a; // this is not possible because int is big but byte is small so we can not transfer it.
    }
}
